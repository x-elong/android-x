<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_edit_profile"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:overScrollMode="never"
        app:layout_constraintTop_toTopOf="parent"/>

    <Button
        android:id="@+id/btn_edit_profile"
        android:layout_width="@dimen/general_224"
        android:layout_height="@dimen/general_38"
        android:layout_marginTop="@dimen/top_30"
        android:background="@drawable/btn_style3"
        android:text="确认提交"
        android:textColor="@color/black"
        android:textSize="@dimen/font_tv_hint_14"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/rv_edit_profile" />
</androidx.constraintlayout.widget.ConstraintLayout>